[pytest]
testpaths = tests
markers =
    integration: live end-to-end tests that call Azure (opt-in; run with -m integration)
# Fast unit tests by default; live tests are opt-in to avoid Azure cost on every run.
addopts = -m "not integration"
filterwarnings =
    ignore::DeprecationWarning
